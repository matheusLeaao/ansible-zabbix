---
- hosts: all
  vars:
    zabbix_server_ip: 'SERVERIP'
    zabbix_hostmetadata: 'METADATA'
  become: yes
  roles:
    - zabbix-agent
# Utilizar ANSIBLE_HOST_KEY_CHECKING=False no começo do comando